[[questions]]
id = "c753bc38-0dc1-44d5-8b06-136fdbbc26d2"
type = "MultipleChoice"
prompt.prompt = """
Какой профиль используется по умолчанию, когда вы запускаете `cargo build` без дополнительных флагов?
"""
answer.answer = "`dev`"
prompt.distractors = [
    "`release`",
    "`build`",
]
context = """
Чтобы минимизировать время компиляции, по умолчанию используется профиль `dev`. Добавьте `--release`, если вам нужен профиль `release` для более долгой компиляции, но более быстрого выполнения кода. (Профиля `build` не существует).
"""