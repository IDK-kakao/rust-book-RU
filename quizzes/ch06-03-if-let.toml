[[questions]]
id = "b2e05b2a-1f98-4498-9a9b-e1e358adf476"
type = "MultipleChoice"
prompt.prompt = """
Какой конструктор управления потоком будет наиболее идиоматичным для использования в следующей функции?

```
enum Location {
  Point(i32),
  Range(i32, i32)
}

fn print_range_max(loc: &Location) {  
  // напечатать второе поле Range, если loc является Range
}
```
"""
answer.answer = "`if let`"
prompt.distractors = [
  "`match`"  
]
context = """
Если функция имеет эффект только в одном условии, то `if let` является наиболее идиоматичным.
"""

[[questions]]
id = "fe17286b-67ed-43a7-a1df-10afee7921cc"
type = "MultipleChoice"
prompt.prompt = """
Какой конструктор управления потоком будет наиболее идиоматичным для использования в следующей функции?

```
enum Location {
  Point(i32),
  Range(i32, i32)
}

fn get_start(loc: &Location) -> i32 { 
  // вернуть первое поле Range или единственное поле Point  
}
```
"""
answer.answer = "`match`"
prompt.distractors = [
  "`if let`",
]
context = """ 
Если функция должна возвращать значение для каждого условия, то `match` является наиболее подходящим.
"""